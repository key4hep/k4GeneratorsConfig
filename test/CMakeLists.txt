add_test(NAME InputAndRun COMMAND ${CMAKE_CURRENT_SOURCE_DIR}/check.sh -r)
set_tests_properties(InputAndRun
  PROPERTIES
    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
    ENVIRONMENT "K4GENERATORSCONFIG=${PROJECT_BINARY_DIR}/bin"
    TIMEOUT 3500
)
